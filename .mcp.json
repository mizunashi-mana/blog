{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp"
      ]
    },
    "safe-file-delete": {
      "command": "npx",
      "args": [
        "@mizunashi_mana/safe-file-deletion-mcp",
        "--allowed-directories=.",
        "--protected-patterns=.git,.claude,.devenv,node_modules,output"
      ]
    }
  }
}
